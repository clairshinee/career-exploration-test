window.__careerCompassV4Parts=window.__careerCompassV4Parts||[];window.__careerCompassV4Parts.push("yView=(state.stage>0&&state.stage<12)?'resume':'',dragState=null,suppressClickUntil=0,analysisTimer=null;\nfunction loadState(){try{let raw=localStorage.getItem(STORAGE_KEY),migrated=false;if(!raw){raw=localStorage.getItem(LEGACY_STORAGE_KEY);migrated=!!raw}if(!raw)return freshState();const old=JSON.parse(raw);if(!old||![3,4].includes(old.version)||!old.selections)throw Error('format');const f=freshState();Object.assign(f,old,{version:4});for(const k of Object.keys(f.selections)){f.selections[k]=Array.isArray(old.selections[k])?old.selections[k].filter(id=>byId[id]):[];const max=limits[k]||99;if(f.selections[k].length>max)f.selections[k]=f.selections[k].slice(0,max);}f.skillEvidence=Object.fromEntries(Object.entries(old.skillEvidence||{}).filter(([id,v])=>f.selections.ability.includes(id)&&[1,2,3].includes(v)));if(!Array.isArray(f.questionIds))f.questionIds=[];if(!f.conflicts||typeof f.conflicts!=='object')f.conflicts={};if(!Array.isArray(f.followups))f.followups=[];if(!f.depth||typeof f.depth!=='object')f.depth={};f.stage=Math.max(0,Math.min(12,Number(f.stage)||0));if(migrated){notice='已沿用你先前保存在此瀏覽器的探索進度。';try{localStorage.setItem(STORAGE_KEY,JSON.stringify(f))}catch(e){}}return f;}catch(e){notice='無法讀取原本的探索進度，已安全回到首頁；原資料不會主動刪除。';return freshState();}}\nfunction saveState(){state.updatedAt=new Date().toISOString();try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));}catch(e){notice='此瀏覽器目前無法儲存進度；完成前請不要關閉頁面。';}showNotice();}\nfunction showNotice(){const el=document.getElementById('notice');if(!el)return;el.textContent=notice;el.classList.toggle('hidden',!notice);}\nconst esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c]));\nfunction selected(k){return state.selections[k].map(id=>byId[id]).filter(Boolean)}\nfunction names(k){return selected(k).map(c=>c.name).join('、')}\nfunction invalidateDerived(){state.questionIds=[];state.conflictIndex=0;state.conflicts={};state.followups=[];state.depth={};state.completedAt=null;}\nfunction validateName(v){const n=String(v||'').trim(),len=Array.from(n).length;return !n?'先告訴我怎麼稱呼你吧 😊':len<2||len>20?'名字請保留在 2～20 個字元之間 😊':'';}\nfunction toast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.remove('hidden');clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.add('hidden'),2500);}\nfunction openName(){entryView='name';if(!state.startedAt)state.startedAt=new Date().toISOString();saveState();render();setTimeout(()=>document.getEleme");
